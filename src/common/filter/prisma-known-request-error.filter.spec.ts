import { PrismaKnownRequestErrorFilter } from './prisma-known-request-error.filter';

describe('PrismaKnownRequestErrorFilter', () => {
  it('should be defined', () => {
    expect(new PrismaKnownRequestErrorFilter()).toBeDefined();
  });
});
