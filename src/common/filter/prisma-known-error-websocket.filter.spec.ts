import { PrismaKnownErrorWebsocketFilter } from './prisma-known-error-websocket.filter';

describe('PrismaKnownErrorWebsocketFilter', () => {
  it('should be defined', () => {
    expect(new PrismaKnownErrorWebsocketFilter()).toBeDefined();
  });
});
